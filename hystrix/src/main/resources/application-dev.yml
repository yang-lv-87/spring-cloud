spring:
  application:
    name: ribbon-consumer
server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka
      
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream